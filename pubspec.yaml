name: ella
description: A a Flutter project bootstrapper that generates a starter Flutter project with all of my opinions.
version: 0.0.1
repository: https://github.com/Crazelu/ella

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  yaml: ^3.1.2
  json2yaml: ^3.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
